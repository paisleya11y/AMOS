# AMOS 使用手册

> AMOS（ACE Multi-Agent Operations System）— TikTok Shop 多 Agent 运营决策副驾驶

---

## 迭代版本

| 版本 | 日期 | 迭代内容 | 核心变化 |
|------|------|----------|----------|
| **v0.1 MVP** | 2026-04 | 单 Agent 原型：选品建议 + 内容策略 + 投放赋能三个独立 API，各自调用 LLM 输出 JSON | 验证"多 Agent 串联"可行性 |
| **v0.2** | 2026-04 | 增加 Benchmark 对标、Diagnose 综合诊断、Validate 质量审核；引入 Skill 文件定义 Agent 专业知识边界 | Agent 从 3 个扩展到 7 个，形成完整流水线 |
| **v0.3** | 2026-05 | 引入 RAG 知识检索（规则打分 + Embedding 余弦相似度混合检索）；增加 5 条知识库条目 | 建议可溯源，减少幻觉 |
| **v0.4** | 2026-05 | Benchmark 对标引入子品类标签匹配维度；增加 Gap 分析（5 维差异化对比）；去重逻辑（自匹配跳过） | 对标从"照搬标杆做法"升级为"差异化适配" |
| **v0.5** | 2026-05 | 选品模块拆分为"现有货盘诊断"+"趋势机会分析"；引入 SKU 级数据（23 个 SKU）；规则引擎（结构/毛利/周转/场域四维诊断）+ 趋势信号匹配 | 选品从"拍脑袋"变成"有数据依据" |
| **v0.6（当前）** | 2026-05 | 选品 prompt 轻量化重构：LLM 只输出文本字段，规则引擎负责数值/数组；maxTokens 扩容至 3000；全链路兜底机制 | **解决选品分析跑不出结果的致命 Bug**；每一层都有确定性的规则化输出兜底 |

---

## 一、产品一句话定位

**AMOS 是面向 TikTok Shop AM（客户经理）的多 Agent 运营决策助手，通过"规则引擎打底 + LLM 增强 + RAG 知识溯源"的混合架构，为商家生成可执行、可溯源、可量化的 7 步诊断报告。**

---

## 二、背景与问题洞察

### 1. 痛点来源

| 痛点 | 现状描述 | 影响 |
|------|----------|------|
| **信息过载** | AM 同时管理 10-30 个商家，每个商家涉及选品、内容、投放、对标等多个维度，TikTok Shop 后台数据分散在多个页面 | 决策靠经验，"感觉"选品，"拍脑袋"定预算 |
| **知识断层** | 平台最佳实践、ACE 方法论白皮书、大促日历等知识分散在文档和培训材料中，AM 难以实时查阅 | 新手 AM 上手周期长（2-3 个月），资深 AM 凭记忆操作 |
| **建议同质化** | 不同类目、不同阶段的商家收到类似的运营建议（"多投达人""加预算"），缺乏定制化 | 商家换 AM 后感受不到差异，AM 价值感低 |
| **趋势盲区** | TikTok 平台搜索热词、内容趋势每分钟在变，AM 无法实时跟踪品类趋势 | 错失蓝海品类窗口期，选品滞后 2-4 周 |
| **缺乏量化基准** | AM 不知道"做得好是什么样"，同行同阶段商家的 GMV、ROI、SKU 结构等无对标参考 | 无法向商家证明"你需要追赶到什么水平" |

### 2. 竞品分析

| 维度 | AMOS | TikTok Shop 原生后台 | 第三方 ERP（如领星、店小秘） | 通用 AI 助手（ChatGPT/Claude） |
|------|------|---------------------|---------------------------|-------------------------------|
| **数据来源** | Mock 数据（可接入 API）+ 趋势信号 + ACE 知识库 | 商家真实经营数据 | 商家授权店铺数据 | 无数据接入 |
| **分析深度** | 7 维度联动诊断（选品→内容→投放→对标→诊断→验证） | 单指标查询（GMV、转化率等） | 进销存 + 财务为主 | 通用问答，无电商领域深度 |
| **建议定制化** | 基于商家 SKU、阶段、品类标签 + 趋势 + 日历的个性化建议 | 无建议能力 | 规则化预警（库存不足等） | 通用建议，缺乏行业知识 |
| **可溯源性** | 每条建议标注 basedOn（缺口/趋势/日历/结构优化）+ 知识来源 | — | — | 无溯源 |
| **输出格式** | JSON + 飞书卡片消息 + 前端可视化报告 | 后台页面 | Excel/报表导出 | 纯文本 |
| **适用角色** | AM（客户经理） | 商家自运营 | 商家/仓库/财务 | 所有人 |
| **差异化** | **多 Agent 协作 + RAG 知识注入 + 规则兜底**，专为 TikTok Shop AM 场景设计 | 官方工具，无分析能力 | 通用进销存，不覆盖内容/投放/选品建议 | 缺乏 TikTok Shop 领域知识和实时数据 |

---

## 三、产品设计

### 1. 核心工作流

```
AM 选择商家 → Router（识别商家画像）
  → Assortment（选品货盘：规则引擎诊断 SKU + 趋势信号匹配）
  → Content（内容策略：内容分型推荐 + 达人 Brief + 直播结构）
  → Empowerment（投放赋能：广告工具选择 + 预算建议 + 大促备战）
  → Benchmark（竞品对标：5 维匹配 + Gap 分析 + 可执行步骤）
  → Diagnose（综合诊断：健康评分 + 阶段判定 + 告警汇总）
  → Validate（质量审核：独立 LLM 审计 → 矛盾检测 → 置信度打分）
→ 输出：前端报告 + 飞书卡片消息
```

**关键设计决策：**

- **串行而非并行**：每个 Agent 依赖前一个 Agent 的输出上下文（如 Diagnose 汇总 Assortment + Content + Empowerment 的结果），因此采用串行流水线
- **规则先行，LLM 增强**：选品、对标、异常检测三个模块先跑规则引擎（确定性计算），LLM 只负责"在规则结果之上写个性化建议"
- **每一层都有兜底**：LLM 调用失败不阻断流水线，规则引擎结果可直接作为最终输出

### 2. 输出渠道

| 渠道 | 格式 | 用途 |
|------|------|------|
| **Web 前端报告** | 可视化卡片 + 颜色编码 + 评分条 | AM 日常分析、团队内部分享 |
| **飞书卡片消息** | 交互式卡片（健康分 + 告警 + Top 行动） | 一键推送到商家群 |
| **API JSON** | 结构化 JSON（FullReport） | 下游系统集成、自动化周报 |

### 3. 核心交互设计决策

#### 决策一：为什么是"一键生成"而不是"对话式"？

**选择**：AM 点击商家 → 一键生成完整 7 步报告，而非 Chatbot 式多轮对话。

**理由**：
- AM 的工作场景是**周报生成**，需要的是"快速出结果"而非"逐步探索"
- 7 步流水线内部已经做了大量推理（RAG 检索、规则诊断、对标匹配），每轮对话都在消耗 API 成本
- 一键生成的确定性输出更利于 AM 建立信任——每次同样的输入得到可预期的结构化结果

**未来方向**：在报告产出后，支持针对特定模块的追问（如"展开讲讲氮化镓充电器的切入策略"）

#### 决策二：规则引擎和 LLM 的分工边界

**选择**：规则引擎负责"确定性计算"（数值、分类、匹配），LLM 负责"自然语言生成"（诊断文本、个性化建议）。

```
规则引擎的职责（不依赖 LLM，100% 可复现）:
  - SKU 分类：星品/风险品识别（isStar、turnoverDays > 60、marginRate < 0.30）
  - 货盘结构：常青品/季节品/测试品占比 → 健康度判定
  - 毛利/周转：均值计算 + 异常品列表
  - 场域诊断：达人/自制/商城搜索占比 → 依赖度判定
  - 趋势匹配：标签重叠过滤 + 相关度排序 + 竞争度分级
  - 对标匹配：5 维加权打分 + 自匹配去重 + Gap 计算
  - 异常检测：GMV 周环比降幅、ROI、SPS、视频数阈值判定

LLM 的职责（需要语言理解和推理能力）:
  - 将规则诊断结果改写成符合商家阶段的、自然的诊断文本
  - 为趋势关键词生成结合商家 SKU 特点的个性化切入建议
  - 综合货盘缺口 + 趋势信号 + 营销日历，生成跨维度的产品推荐
  - 判断建议的 urgency（高/中/低）和 basedOn 来源分类
```

**为什么这样划分？**
- 规则引擎的结果**可审计**——AM 可以反查"为什么这个 SKU 被标记为风险品"（因为 turnoverDays > 60）
- LLM 的结果**不可控但可约束**——通过 prompt 中的枚举值限定、来源标注要求、urgency 判定规则来约束 LLM 行为
- 当 LLM 不可用时，规则引擎结果**自足可用**——这是兜底设计的核心

#### 决策三：RAG 知识的注入时机和方式

**选择**：在每个 Agent 的 prompt 构建时，检索相关知识并注入到 prompt 的 `【检索到的相关知识】` 段落。

**检索策略**：混合打分（规则 40% + 语义 60%），取 Top 3。

**为什么是 prompt 注入而不是 fine-tuning？**
- 知识库条目（ACE 白皮书、平台公告）会持续更新，prompt 注入可以零成本更新知识
- fine-tuning 需要标注数据且更新周期长（重新训练），不适合快速迭代的运营场景
- 每条建议标注"来源：XXX"，让 AM 可以判断建议的可信度

#### 决策四：Validate Agent 的作用

**选择**：在流水线末尾增加一个独立的 LLM 调用，以"质量审计员"角色检查前 6 个 Agent 的输出。

**检查项**：数据一致性（GMV 数字前后矛盾）、阶段匹配（cold_start 商家收到了 mature 阶段的建议）、过度激进（ROI 低于 1.5 还建议加预算）

**为什么不放在 Diagnose Agent 内部？**
- 独立 Agent 调用不会受 Diagnose prompt 中的其他任务干扰
- 独立的 `temperature` 设置（更保守的审计模式）

---

## 四、技术架构

### 1. 四层系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                    展示层（Presentation）                      │
│  Next.js 16 App Router + React 组件                          │
│  AgentTrace（执行追踪）| ReportCard（报告展示）                  │
│  AlertPanel（告警）| MerchantSelector（商家选择）| LarkPreview  │
├─────────────────────────────────────────────────────────────┤
│                    编排层（Orchestration）                     │
│  page.tsx runAgents() — 7 步串行流水线                        │
│  每个步骤：发起 API → 重试（最多 3 次）→ 更新 Trace → 兜底     │
│  最终：组装 FullReport → 渲染报告 + 飞书发送                   │
├─────────────────────────────────────────────────────────────┤
│                     Agent 层（Multi-Agent Core）               │
│  7 个 API Route（POST）每个对应一个 Agent                      │
│  Router → Assortment → Content → Empowerment → Benchmark      │
│  → Diagnose → Validate                                       │
│  每个 Agent = System Prompt + Skill 文件 + RAG 知识 + LLM     │
├─────────────────────────────────────────────────────────────┤
│                    能力层（Capabilities）                      │
│  规则引擎：anomalyDetector | inventoryAnalyzer |               │
│           benchmarkMatcher | calendarTool                     │
│  RAG 系统：knowledgeBase + embeddings + retriever              │
│  Memory：merchantProfile（历史画像）+ amPreference（偏好）      │
│  LLM 客户端：callDeepSeek / callDeepSeekJSON（重试+兜底）      │
│  外部集成：Lark/飞书 API                                      │
├─────────────────────────────────────────────────────────────┤
│                    数据层（Data）                              │
│  Mock 数据：3 个商家 | 23 个 SKU | 14 个趋势信号               │
│          5 个标杆案例 | 6 个大促节点 | 5 条知识条目            │
│  （生产环境可替换为 TikTok Shop API + 数据库）                  │
└─────────────────────────────────────────────────────────────┘
```

### 2. 七大 Agent 模块

#### Agent 0：Router（路由 Agent）

| 项目 | 说明 |
|------|------|
| **职责** | 识别商家画像（品类、阶段、GMV），确定后续 6 个 Agent 的分析参数 |
| **技术实现** | 前端模拟（400ms 延迟），不做 LLM 调用 |
| **输入** | merchantId |
| **输出** | 商家名称、品类、阶段确认 |
| **关键约束** | 无（纯数据读取） |

#### Agent 1：Assortment（选品货盘 Agent）

| 项目 | 说明 |
|------|------|
| **职责** | 现有货盘诊断 + 趋势品机会分析 + 综合产品推荐 |
| **技术实现** | **规则引擎 + LLM 增强** |
| **规则引擎部分** | `analyzeInventory(skuList)`：遍历 23 个 SKU，计算星品/风险品、结构健康度（常青 45-75%、季节 20-40%、测试 5-15%）、毛利健康度（低毛利 < 35%）、周转健康度（滞销 > 45 天）、场域诊断（达人依赖度）；`getTrendOpportunities(subCategoryTags)`：按标签过滤 14 个趋势信号，按相关度排序取 Top 6 |
| **LLM 增强部分** | 输出 4 段诊断文本（结构/毛利/周转/场域）+ 每个趋势词的个性化 suggestedAction + 3-5 个产品推荐（含 basedOn 溯源字段） |
| **兜底机制** | `buildFallbackResult()`：当 LLM 不可用时，基于规则引擎结果生成缺口推荐和预警，确保选品模块始终有输出 |
| **RAG 知识** | 检索与商家品类和阶段相关的 ACE 方法论知识（如 "cold_start 只聚焦 1-2 个爆品"） |
| **输出示例** | 8 个 SKU → 3 个星品 + 2 个风险品 → 结构诊断"季节品占比仅 13%，远低于 growth 阶段建议的 30%"→ 4 个趋势推荐 + 4 个产品推荐 |

#### Agent 2：Content（内容策略 Agent）

| 项目 | 说明 |
|------|------|
| **职责** | 基于品类和阶段推荐内容分型、达人合作 Brief、直播结构、周发布计划 |
| **技术实现** | LLM 生成（prompt 中含 5 大内容分型参考表 + 阶段目标） |
| **输入** | 商家品类、阶段、周视频/直播数据、达人/自制 GMV 占比 |
| **输出** | `recommendedFormats[]`（含 type/reason/exampleAngle）、`creatorBrief`、`liveStructure`、`weeklyPlan` |
| **关键约束** | 3C/工具类优先推荐"开箱直拍型""卖点讲解型"；美妆/个护优先推荐"买家实测型""生活场景型"（System Prompt 中硬编码） |
| **兜底机制** | 3 次重试 + 本地回退数据（默认内容分型 + 标准 Brief + 通用直播结构） |

#### Agent 3：Empowerment（投放赋能 Agent）

| 项目 | 说明 |
|------|------|
| **职责** | 广告工具选择 + 预算建议 + 大促备战提醒 |
| **技术实现** | LLM 生成 + 规则异常检测（`detectAnomalies()`） |
| **输入** | 商家广告数据（ROI、投放占比）、异常告警、8 周内大促节点 |
| **输出** | `adStrategy`、`recommendedTools[]`、`campaignNode`、`weeklyBudgetSuggestion` |
| **关键约束** | 工具选择有硬编码规则：ROI < 1.5 + 冷启期 → Promote（非 GMV Max）；有优质达人素材 → Spark Ads；大促前 14 天 → Live GMV Max |
| **设计亮点** | System Prompt 中预设的工具选择规则让 LLM 的行为可预测，而非完全"自由发挥" |

#### Agent 4：Benchmark（竞品对标 Agent）

| 项目 | 说明 |
|------|------|
| **职责** | 从 5 个标杆案例中匹配最相近的商家，生成差异化可执行步骤 |
| **技术实现** | **规则引擎匹配 + LLM 增强** |
| **规则引擎部分** | `matchBenchmark(merchant)`：5 维加权打分——子品类标签重叠（20 分）+ 品类类型（20 分）+ 一级类目（15 分）+ 经营阶段（25 分）+ GMV 量级（20 分）→ 自匹配去重 → `computeGap()` 输出多维度 Gap 分析 |
| **LLM 增强部分** | 基于匹配结果和 Gap 数据，生成"为什么相似""差距在哪""怎么补"的自然语言描述 + 可执行步骤 + 量化预期 |
| **输出示例** | Bnker（月 GMV $420K，3C 音频充电）匹配到 JBL Audio → 相似度 82% → Gap：GMV 高于 JBL 同期 40% → 建议：参考 JBL 的多场域均衡策略，将自制内容 GMV 占比从 20% 提升到 30% |
| **关键设计** | 不是"把标杆的做法照搬过来"，而是"分析差距后给出适配建议" |

#### Agent 5：Diagnose（综合诊断 Agent）

| 项目 | 说明 |
|------|------|
| **职责** | 汇总前 5 个 Agent 结果，产出健康评分、阶段判定、综合告警 |
| **技术实现** | LLM 生成 + 规则异常检测合并 |
| **输入** | 商家全量数据 + 周度趋势 + 历史画像 + 异常检测结果 + 大促日历 |
| **输出** | `healthScore`（0-100）、`stage`、`weakestDimension`（assortment/content/empowerment）、`stageSummary`、`alerts[]` |
| **评分权重** | SPS 30% + ROI 25% + GMV 增长 25% + 内容产出 20%（Skill 文件中定义） |
| **设计亮点** | `result.alerts = [...llmAlerts, ...anomalyAlerts]` — LLM 告警和规则告警取并集，不遗漏 |

#### Agent 6：Validate（质量审核 Agent）

| 项目 | 说明 |
|------|------|
| **职责** | 独立审计前 6 个 Agent 的输出，检测矛盾、过度激进/保守、数据不一致 |
| **技术实现** | 独立 LLM 调用（不与其他 Agent 共享 prompt） |
| **输入** | 商家数据 + 完整报告 + 健康评分 |
| **输出** | `overallConfidence`（0-100）、`issues[]`、`suggestionScores[]`、`revisedSuggestions[]` |
| **典型检测** | ROI 低于 1.5 却建议加预算 → 标记矛盾；cold_start 商家建议扩展 10+ SKU → 标记过度激进 |
| **设计决策** | 作为独立 Agent 而非 Diagnose 的子步骤，避免 prompt 内部的注意力稀释 |

### 3. Memory 三层架构

```
┌─────────────────────────────────────────────┐
│           L1：实时上下文（Prompt 注入）         │
│  - 当前商家数据（MerchantData）                 │
│  - 本周 SKU 数据（SkuData[]）                  │
│  - RAG 检索结果（Top 3 知识条目）               │
│  - 大促日历（近 8 周活动）                      │
│  → 每个 Agent 的 prompt 构建时动态注入          │
├─────────────────────────────────────────────┤
│           L2：短期记忆（Memory 文件）            │
│  - 商家历史画像（近 3 周 healthScore、          │
│    已执行建议、趋势方向）                         │
│  - AM 偏好（语气风格、关注维度、编辑历史）        │
│  → merchantProfile.ts / amPreference.ts        │
│  → 用于 Diagnose Agent 的纵向对比分析            │
├─────────────────────────────────────────────┤
│           L3：长期知识（RAG 向量库）             │
│  - ACE 方法论白皮书（5 条结构化知识）            │
│  - 平台政策/公告（SPS 分数规则等）               │
│  - 行业数据（美妆品类基准等）                     │
│  → 支持 Embedding 语义检索 + 规则打分混合        │
│  → 每条知识可溯源（source、verified 标记）       │
└─────────────────────────────────────────────┘
```

**Memory 更新策略**（规划中）：
- L1：每次报告生成时全新计算
- L2：报告生成完成后，将本次健康分、已生成建议写回 profile
- L3：人工维护知识库文件，支持热更新（无需重启服务）

---

## 五、产品简介

### 1. 产品全景图

```
                          ┌──────────────┐
                          │   AM 用户     │
                          └──────┬───────┘
                                 │
                   选择商家 → 一键生成报告
                                 │
          ┌──────────────────────┼──────────────────────┐
          ▼                      ▼                      ▼
    ┌──────────┐          ┌──────────┐          ┌──────────┐
    │  Web 前端 │          │ API JSON │          │ 飞书卡片  │
    │ 诊断报告  │          │ 结构化输出│          │ 群消息    │
    └──────────┘          └──────────┘          └──────────┘
                                 │
          ┌──────────────────────┼──────────────────────┐
          │        7-Agent 串行流水线                     │
          │  Router → Assortment → Content →             │
          │  Empowerment → Benchmark → Diagnose →        │
          │  Validate                                    │
          │                                              │
          │  ┌──────────────────────────────────────┐    │
          │  │  3 层 Prompt 工程                      │    │
          │  │  System Prompt + Skill 文件 + RAG 知识 │    │
          │  └──────────────────────────────────────┘    │
          │                                              │
          │  ┌──────────────────────────────────────┐    │
          │  │  混合计算引擎                           │    │
          │  │  规则引擎（确定） + LLM（生成）          │    │
          │  │  → 每层均有兜底                          │    │
          │  └──────────────────────────────────────┘    │
          └──────────────────────────────────────────────┘
                                 │
          ┌──────────────────────┼──────────────────────┐
          │                      │                      │
          ▼                      ▼                      ▼
    ┌──────────┐          ┌──────────┐          ┌──────────┐
    │ Mock 数据 │          │ RAG 知识库│          │ DeepSeek │
    │ 3 商家    │          │ 5 条知识  │          │ API      │
    │ 23 SKU   │          │ 256 dim   │          │ v4-flash │
    │ 14 趋势   │          │ embedding │          │          │
    └──────────┘          └──────────┘          └──────────┘
```

### 2. 产品能力

| 能力模块 | 核心功能 | 输入 | 输出 |
|----------|----------|------|------|
| **选品货盘** | SKU 级货盘诊断 + 趋势品机会挖掘 | 商家 SKU 数据 + 品类标签 | 星品/风险品清单、结构/毛利/周转/场域诊断、趋势品推荐、综合产品建议 |
| **内容策略** | 内容分型推荐 + 达人 Brief + 直播结构 + 周计划 | 商家品类 + 阶段 + 视频/直播数据 | 推荐内容分型、达人合作 Brief、直播排品结构、周发布节奏 |
| **投放赋能** | 广告工具选择 + 预算建议 + 大促备战 | 商家广告数据 + 异常告警 + 大促日历 | 广告策略文本、推荐工具列表、大促节点提醒、周预算建议 |
| **竞品对标** | 5 维标杆匹配 + Gap 分析 + 可执行步骤 | 商家画像 + 标杆案例库 | 最佳匹配标杆、相似度评分、差距分析、复刻步骤 + 量化预期 |
| **综合诊断** | 健康评分 + 阶段判定 + 告警汇总 | 前 4 个 Agent 结果 + 历史数据 | 0-100 健康分、经营阶段、最薄弱维度、阶段总结、分级告警 |
| **质量审核** | 独立 LLM 审计 + 矛盾检测 | 商家数据 + 完整报告 | 置信度评分、问题清单、修正建议 |
| **报告输出** | Web 可视化 + 飞书卡片 | FullReport JSON | 前端报告页 + 飞书群卡片消息 |

### 3. 产品使用说明

#### a) 商家数据引入

当前使用 Mock 数据（3 个商家），生产环境替换路径：

```
Mock 数据（当前）                →  生产环境
─────────────────────────────────────────────────
mockData/merchants.ts           →  TikTok Shop API / 数据库
mockData/skuData.ts             →  商家商品 API（实时库存、价格、销量）
mockData/categoryTrends.ts      →  TikTok 热搜 API / 第三方趋势数据
mockData/benchmarkCases.ts      →  商家数据库 + 运营案例沉淀
mockData/marketingCalendar.ts   →  平台大促日历 API
rag/knowledgeBase.ts            →  运营知识库 CMS
memory/merchantProfile.ts       →  数据库（历史报告表）
memory/amPreference.ts          →  数据库（AM 偏好表）
```

**接入步骤**：
1. 在 `src/lib/mockData/` 同级创建 `src/lib/data/` 目录
2. 实现对应数据源的 API 调用函数（保持与 mock 函数相同的函数签名）
3. 在各 API Route 中将 `import from mockData` 替换为 `import from data`
4. 配置环境变量（`DATABASE_URL`、`TIKTOK_API_KEY` 等）

#### b) 产品功能使用引导

**Step 1：环境配置**

```bash
# .env.local
DEEPSEEK_API_KEY=sk-xxxxxxxx    # DeepSeek API 密钥（必需）
LARK_APP_ID=xxx                  # 飞书应用 ID（可选，用于飞书推送）
LARK_APP_SECRET=xxx              # 飞书应用密钥（可选）
LARK_CHAT_ID=xxx                 # 飞书群 ID（可选）
```

**Step 2：启动服务**

```bash
npm install
npm run dev
# 访问 http://localhost:3001
```

**Step 3：生成报告**

1. 左侧边栏选择商家（Insta 365 / TechGadget Pro / Bnker）
2. 点击"生成报告"按钮
3. 右侧 AgentTrace 面板实时显示 7 步执行进度
4. 报告生成后，中间区域展示完整诊断结果：
   - **选品货盘**（青色卡片）：产品推荐含 urgency 标签和 basedOn 溯源
   - **内容策略**（蓝色卡片）：推荐分型 + 达人 Brief + 直播结构 + 周计划
   - **投放赋能**（琥珀色卡片）：广告策略 + 工具标签 + 大促节点 + 预算
   - **竞品对标**（紫色卡片）：匹配标杆 + 相似度 + 差距分析 + 可执行步骤
   - **健康评分**（顶部大号数字）：绿色 ≥ 70 / 黄色 50-70 / 红色 < 50
5. 点击"发送到飞书群"将卡片推送到配置的群聊

**Step 4：理解报告字段**

| 字段 | 含义 | 示例 |
|------|------|------|
| `urgency: high` | 需立即处理 | 风险品清仓、搜索增速 95% 且竞争度低的蓝海品 |
| `urgency: medium` | 中期布局 | 货盘结构调整、趋势品测试 |
| `urgency: low` | 长期关注 | 品类扩展方向 |
| `basedOn: inventory_gap` | 基于货盘缺口 | 缺少测试品、低毛利品过多 |
| `basedOn: trend_signal` | 基于趋势数据 | 氮化镓搜索增速 45%、竞争度低 |
| `basedOn: calendar_event` | 基于营销日历 | 黑五备货、返校季布局 |
| `basedOn: structure_optimization` | 基于结构优化 | 调整常青品占比、优化场域分布 |
| `healthScore` | 综合健康分（0-100） | 85 = 健康 / 55 = 需关注 / 35 = 告警 |

---

## 六、Q&A

### 技术架构类

**Q1：为什么选择 7 个 Agent 串行而不是并行？**

每个 Agent 有依赖关系——Diagnose 需要汇总前几个 Agent 的结果，Validate 需要审计所有 Agent 的输出。串行执行确保每步拿到上一步的完整上下文。但 Assortment / Content / Empowerment 三个 Agent 之间没有强依赖，未来可以并行执行以减少总耗时。

**Q2：RAG 的混合检索具体是怎么做的？规则打分 40% 和语义 60% 的权重是怎么定的？**

规则打分：类目匹配（+3 分）、阶段匹配（+3 分）、模块关联度（+2 分）、已验证标记（+1 分）→ 归一化到 [0, 1]。语义打分：将商家画像和知识条目分别做 Embedding → 余弦相似度 → 也归一化到 [0, 1]。最终 `hybrid = rule × 0.4 + semantic × 0.6`。

权重选择是工程权衡：40% 规则保证至少能匹配到相关条目（不跑偏），60% 语义让检索更灵活（能发现表面不匹配但内容相关的知识）。实际效果需要在大量商家数据上做召回率测试来调优。

**Q3：Embedding 是怎么生成的？**

优先调用 DeepSeek Embedding API，如果 API Key 未配置或调用失败，自动降级为**本地关键词哈希向量**（`simpleEmbed`）：对中文文本做分词 → 每个词通过字符串哈希映射到 256 维向量 → L2 归一化。这种设计保证 RAG 系统在没有外部 API 的情况下仍然可用。

**Q4：LLM 生成的 JSON 经常被截断怎么办？**

已通过 v0.6 迭代解决：选品模块的 LLM 现在只输出轻量文本字段（4 段诊断 + 趋势 action + 推荐列表），约 500-800 tokens。所有大数据（starSkus、riskSkus、数值字段）由规则引擎计算、服务端合并。`maxTokens` 也从默认 2000 提高到 3000。

**Q5：如果 DeepSeek API 彻底不可用，系统还能工作吗？**

能。选品模块有完整的规则化兜底（`buildFallbackResult`），内容模块有本地回退数据。只有对标和综合诊断两个模块的 LLM 部分会缺失，但规则引擎输出的匹配结果和异常检测仍然会展示。

### 业务合理性类

**Q6：Benchmark 对标会不会把不相关的商家匹配到一起（比如充电宝商家匹配到美发器商家）？**

v0.4 已修复这个问题。引入了**子品类标签重叠维度**（占 20 分权重）：两个商家的 `subCategoryTags` 取交集，重叠越多分越高。同时增加了**自匹配去重**：如果最佳匹配就是商家自己，自动跳到第二名。当前的 5 个标杆案例覆盖了 3C 音频充电和影像两个品类，同类目内匹配准确。

**Q7：选品建议的定制化体现在哪里？和直接用 ChatGPT 问"给我推荐几个产品"有什么区别？**

三个层面：
1. **数据层面**：基于商家真实 SKU 数据做诊断（不是通用的"3C 应该卖什么"），知道哪个 SKU 滞销、哪个毛利过低
2. **知识层面**：RAG 注入了 TikTok Shop ACE 方法论和平台规则，建议不会违背平台最佳实践
3. **溯源层面**：每条建议标注 `basedOn`（缺口/趋势/日历/结构优化）和知识来源，AM 可以判断建议的依据是否可靠

**Q8：规则引擎和 LLM 的诊断结论如果冲突怎么办？**

目前 LLM 的诊断文本是基于规则引擎结果生成的（prompt 中已包含规则诊断结论），所以不存在根本性冲突。但如果 LLM 生成了与规则数据不一致的内容（比如规则说"毛利健康"但 LLM 说"毛利偏低"），Validate Agent 会标记出来。未来的迭代可以加入更强的校验逻辑。

### 使用场景类

**Q9：这个工具是给谁用的？商家自己能用吗？**

目标用户是 AM（Account Manager，客户经理），不是商家。AM 管理 10-30 个商家，需要快速生成周报、发现问题商家、给出运营建议。AMOS 输出的是"给 AM 的诊断参考"，AM 可以基于此调整后发给商家。商家不直接使用。

**Q10：每周都要重新生成报告吗？历史报告怎么查看？**

当前版本每次生成都是全新计算。Memory 层（`merchantProfile`）保存了近 3 周的历史健康分和已执行建议，用于 Diagnose Agent 做纵向对比。历史报告的持久化和回看功能在规划中。

**Q11：如果我想添加新的 Agent（比如物流分析、客服分析），怎么做？**

步骤：
1. 在 `src/types/index.ts` 定义新的 Result 类型
2. 在 `skills/` 创建 Skill 文件（定义 Agent 专业知识边界）
3. 在 `src/lib/prompts/` 创建 Prompt 构建函数
4. 在 `src/app/api/` 创建 API Route
5. 在 `src/app/page.tsx` 的 `runAgents()` 中添加步骤
6. 在 `src/components/agent/ReportCard.tsx` 中添加展示区域

### 评估与改进类

**Q12：怎么评估 Agent 输出质量？**

四个维度：
1. **输出质量**（可读、具体、可执行、可溯源）
2. **业务效果**（建议采纳率、GMV 提升、不良率压降）
3. **系统可靠**（响应时间 ≤ 60s、可用率 ≥ 99.5%）
4. **AM 行为**（周报生成频次、建议修改率）

当前系统只覆盖了维度 1（Validate Agent 的置信度评分）和维度 3（兜底机制），维度 2 和 4 需要生产环境实际使用数据。

**Q13：RAG 知识库怎么更新和扩展？**

修改 `src/lib/rag/knowledgeBase.ts` 中的 `knowledgeBase` 数组，添加新的 `KnowledgeEntry`：
```typescript
{
  id: 'kb_006',
  source: 'TikTok Shop 2026Q2 品类趋势报告',
  category: 'fashion',
  stage: 'growth',
  content: '服装品类 Q2 短视频 Top 3 内容分型：穿搭对比(40%)、开箱试穿(35%)、面料细节(25%)',
  dataPoint: '穿搭对比型视频 CTR 平均 4.2%',
  verified: true,
}
```

重启服务后，新嵌入向量会在首次检索时自动计算（`initKnowledgeBaseEmbeddings` 的懒加载机制）。

### 面试相关

**Q14：这个项目最大的技术难点是什么？**

不是单个技术点的难度，而是**多 Agent 协作的工程化**：7 个 Agent 的 prompt 要协调一致（术语统一、约束不冲突）、规则引擎和 LLM 的分工边界要清晰、每一层的兜底要保证流水线不中断、输出格式要统一以方便 Validate Agent 审计。这些在单一 Agent 项目中不会遇到。

**Q15：如果要给这个项目打分（满分 10 分），你会打几分？为什么？**

这取决于评估维度。从**架构设计**角度，7 Agent 的流水线、规则+LLM 的混合模式、RAG 的混合检索、每一层的兜底机制——这些工程决策是合理的，可以打 8 分。从**生产就绪**角度，当前还处于 Mock 数据阶段，缺乏真实 API 接入、用户认证、错误监控、历史数据持久化——大约 5 分。综合来看是一个"设计完整、实现扎实的 POC/原型项目"。
