# 面试逐字稿：TikTok Shop ACE 运营副驾驶

---

## 一、项目一句话定位（30 秒）

> 我做的是一个 **TikTok Shop 商家运营的 AI 决策系统**。它的核心场景是：TikTok 上的客户经理每人管理几十个商家，每周要给每个商家出一份经营诊断报告，包含选品、内容、投流、标杆分析四个维度。我做的系统就是把这个过程自动化——选择一个商家，点一个按钮，7 个 AI Agent 依次执行，30 秒内生成一份带健康评分、具体建议、预警提示的完整报告。

---

## 二、业务背景（1.5 分钟）

**TikTok Shop 是什么模式？**
它和传统货架电商不一样，是"货找人"——商品靠短视频和直播推荐给用户，而不是用户主动搜索。所以商家能不能做好，核心看三件事：

- **Assortment（好商品）**：有没有适合在视频/直播里卖的商品
- **Content（好内容）**：达人视频和自播有没有持续产出
- **Empowerment（好营销）**：广告投放和大促备战有没有做对

这就是 TikTok 官方推的 **ACE 方法论**。

**客户经理的痛点是什么？**
一个 AM 管 50-80 个商家，每周要挨个看数据、写建议、发报告。光是算健康分、匹配标杆案例就要花半天。而且建议的质量完全依赖 AM 个人经验——老手和新手差距很大。

**这个项目要解决的就是：把 AM 的诊断经验系统化、自动化，让 AI 辅助决策，而不是替代决策。**

---

## 三、系统架构总览（2 分钟）

整体一句话：**基于 DeepSeek API 的 Multi-Agent 系统，7 个 Agent 顺序执行，最终生成一份完整诊断报告。**

### 架构三层

```
前端 page.tsx      →  runAgents() 顺序调用 7 个 API，实时更新执行轨迹
API Routes (7 个)   →  每个 Route 注入 Skill + RAG 知识，调用 DeepSeek
工具层              →  rule-based 检测器（异动/标杆匹配/大促日历）做前置处理
```

### 7 步流水线

| 步骤 | Agent | 做什么 |
|------|-------|--------|
| 1 | Router | 识别商家信息 |
| 2 | Assortment | 选品推荐（常青品/季节品/高客单品） |
| 3 | Content | 内容策略（五大分型 + 达人Brief + 直播结构） |
| 4 | Empowerment | 投流策略（工具组合 + 预算分配 + 大促备战） |
| 5 | Benchmark | 标杆案例匹配（4 维度加权打分） |
| 6 | Diagnose | 综合诊断（健康评分 + 最薄弱维度 + 预警） |
| 7 | Validate | 质量审核（逐条打分，标记矛盾，输出修正） |

### 每个 Agent 的 Prompt 怎么构建？

三层叠加：**全局方法论 + Skill 专家人设 + RAG 检索知识**。

关键设计：rule-based 工具做确定性检测（比如 ROI < 1.5 必报警），LLM 做非确定性推理，Validate Agent 做质量兜底。

---

## 四、四个核心技术亮点（5-6 分钟）

### 4.1 Multi-Agent 编排：为什么是 7 个 Agent 而不是一个大 Prompt？

面试官可能会问"为什么不把所有问题放在一个 prompt 里让 GPT 一次性回答"。两个原因：

**第一，专业分工。** 选品、内容、投流是三个完全不同的知识域。选品专家需要知道 SKU 宽度、动销率、品类趋势；投流专家需要知道 ROI 阈值、广告工具适用条件、预算分配逻辑。把五个专家的知识塞进一个 prompt，LLM 容易"串场"——用选品逻辑去回答投流问题。

**第二，Pipeline 依赖。** Diagnose（综合诊断）需要拿到前四个 Agent 的结果才能判断"最薄弱的是哪个维度"。Validate 需要所有输出才能做审核。所以顺序执行是合理的。

具体编排方式很简单：前端 `runAgents()` 函数里，一个接一个的 `fetch + updateStep`，每步结束更新执行轨迹面板的状态。没有用消息队列或任务调度框架，就是直接的 async/await 链——因为当前是单用户场景，够用且易理解。

### 4.2 RAG + Skill 注入：让建议"有据可查"

这是我针对"AI 幻觉"问题的设计。

**RAG 部分：** 我建了一个知识库 `knowledgeBase`，每一条知识包含来源（白皮书第几节 / 哪个真实案例 / 平台公告）、适用品类、适用阶段、具体数据点。每个 Agent 调 `retrieveRelevantKnowledge()` 按品类+阶段+模块相关性打分，取 Top 3，注入到 prompt 里。关键约束：prompt 里明确写"每条建议必须标注来源，格式为「建议内容（来源：XXX）」"。这样面试官看到的建议不是"AI 编的"，而是"基于 ACE 白皮书第 3.2 节 + JBL 案例数据"。

**Skill 部分：** 每个 Agent 在启动时通过 `fs.readFileSync` 加载对应的 `skills/*.md` 文件，直接拼到 System Prompt 后面。每个 Skill 文件定义了四个东西：

- **专业背景**：量化经验——"管理过 500+ 商家账户"
- **判断标准**：硬性阈值——"ROI < 1.5 立即预警，ROI > 3.0 加大 GMV Max"
- **不会做的事**：负面约束——"不在 ROI 未达标时建议开新广告计划"
- **输出规范**：格式要求 + 具体示例——"每条建议必须包含：当前状态 → 问题诊断 → 具体动作 → 预期效果"

效果是：同样的 DeepSeek 模型，加上 Skill 后输出更聚焦、更规范。面试官如果问"你怎么控制不同 Agent 的输出质量"，就可以从这两个设计讲。

### 4.3 验证 Agent：多 Agent 的质量闭环

如果面试官问"你凭什么相信 AI 的建议是对的"，我就讲这个。

5 个 Agent 跑完后，第 7 步是一个独立的 Validate Agent。它会拿到：

- 商家的原始数据（GMV、ROI、SPS、品类、阶段）
- 前 5 个 Agent 的全部输出
- 健康评分

然后让一个独立的 LLM 调用，以"严格审核员"的角色，检查四件事：
1. 建议是否和商家数据矛盾？比如 ROI 很低却建议加预算
2. 建议是否和商家阶段匹配？比如冷启期不可能建议"多账号矩阵"
3. 每条建议的可信度打分（0-100）
4. 输出修正后的建议

这个设计的意义是：**不用人工审核每份报告，AI 自己先审一遍。** 置信度低的报告，AM 拿到手时可以重点关注。

### 4.4 硬约束工程：防止 LLM 在关键决策上自由发挥

LLM 最大的问题是"创造性幻觉"——它可能在 ROI=1.3 的时候推荐 GMV Max（不符合规则），可能对 3C 品类推荐"买家实测型"内容（品类不匹配）。

我用了三层约束来解决：

**第一层，Rule-based 工具前置。** 在 prompt 到达 LLM 之前，`anomalyDetector` 已经用硬编码的阈值（ROI < 1.5 → critical）生成了 alert。`benchmarkMatcher` 已经用 4 维度加权打分算出了最佳案例。LLM 拿到的不是"请你自己分析"，而是"这里是已经检测到的异常和已经匹配的案例，你基于此给建议"。

**第二层，System Prompt 里的硬性规则。** 比如"ROI < 1.5 且冷启期 → 必须推荐 Promote，严禁推荐 GMV Max"。这些规则不是"建议"，是"必须"。

**第三层，Skill 文件里的负面约束。** 每个 Skill 都有"你不会做的事"部分，比如投流专家"不在 ROI 未达标时建议开新广告计划"、"不在冷启期推荐月消耗>$3000 的方案"。

三层下来形成了一个"围栏"：rule-based 工具框定输入，System Prompt 框定逻辑，Skill 框定输出。

---

## 五、工程实践（1.5 分钟）

几个不展开但要提的点：

**类型安全：** 完整的 TypeScript 类型体系，14 个 interface，从 `MerchantData` 到 `FullReport` 到每个 Agent 的返回类型 `AssortmentResult`、`ContentResult` 等，保证数据流可追溯。

**容错设计：** Content Agent 有 3 次指数退避重试，全失败后用 fallback 数据，不让前端白屏。整个 `runAgents()` 外层有 try/catch，任何一步崩溃都有完整的 fallback report。

**可观测性：** 执行轨迹面板 `AgentTrace` 实时显示每个 Agent 的状态（等待中/执行中/完成/失败）和耗时。每个 API Route 有详细的 console.log，包含处理时长、输入输出摘要。

---

## 六、成果展示 + 反思（1.5 分钟）

**现在可以做的事情：**
选择一个商家 → 点击「生成诊断报告」→ 30 秒内看到：
- 健康评分（0-100）+ 最薄弱维度
- 选品建议（带紧急度和理由）
- 内容策略（带达人 Brief 模板）
- 投流方案（带预算建议）
- 标杆案例匹配（带相似度分数和可复制步骤）
- 验证报告（整体置信度 + 逐条打分）
- 飞书消息预览（一键发送）

**如果再做一次，会改进什么：**
1. 知识库用向量数据库（如 Pinecone）替代内存数组，支持更大规模和语义相似度检索
2. Agent 间通信改成结构化消息传递（每个 Agent 的输出是下一个 Agent 的输入的一部分），现在 Diagnose 只拿到其他 Agent 的最终结果，看不到中间推理过程
3. 加入 AM 反馈闭环：AM 编辑了某条建议后，记录到 `amPreference`，下次生成时参考历史编辑偏好

**可能的扩展方向：**
增加一个"AM 对话 Agent"——AM 可以对某条建议追问"为什么推荐这个品"，Agent 基于 RAG 知识给出解释。这能把单向报告变成双向对话。

---

这个项目演示了：Multi-Agent 编排、RAG 落地、Prompt 工程、验证闭环、容错设计。这些不限于 TikTok Shop，任何需要 AI 辅助专业决策的场景都可以复用这个架构。有什么问题吗？
